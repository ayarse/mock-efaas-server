# ===========================================
# neFaas Mock Server
# ===========================================

# Server
PORT=36445
HOST=localhost
MOCK_PASSWORD=@123456

# Path to a custom users JSON file (optional, uses bundled users.json if empty)
USERS_FILE=
# Path to a custom clients JSON file (optional, uses default mock client if empty)
CLIENTS_FILE=

# ===========================================
# fetch-real-users.ts script (optional)
# Only needed if you want to fetch real user
# data from the eFaas developer environment
# ===========================================

EFAAS_CLIENT_ID=
EFAAS_CLIENT_SECRET=
EFAAS_ISSUER=https://developer.gov.mv/efaas
EFAAS_REDIRECT_URI=
